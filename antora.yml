# component descriptor
name: connected-json
title: Connected JSON
# version: '1.2.3' on other branches
version: v8.0.0
start_page: index.adoc
#    It changes the text in the version picker dropdown.
display_version: '8.0.0'
# 2. Prerelease Flag: Tells Antora this isn't stable.
#    This prevents it from being the "default" page if you have stable versions.
prerelease: false
# prerelease: Draft
asciidoc:
  attributes:
    experimental: ''
    stem: ''
    sectnums: '@'
    icons: 'font'
    idprefix: ''
    idseparator: '_'
    page-pagination: true
  sourcemap: true
nav:
  - modules/ROOT/nav.adoc
